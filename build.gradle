apply plugin:'java'
defaultTasks 'test'

repositories {
	mavenLocal()
	mavenCentral()
	flatDir name:'lib', dirs:'lib'
}

dependencies {
	compile 'lib:disruptor:1.0.4'
	testCompile 'junit:junit:4.8.2'
}
